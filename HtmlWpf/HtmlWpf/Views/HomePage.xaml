<Page
    x:Class="HtmlWpf.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Dashboard">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel HorizontalAlignment="Center" Margin="24">
            <TextBlock Text="Dashboard" Style="{StaticResource PageTitleStyle}" />
            <ItemsControl x:Name="DashboardList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource DashboardButtonStyle}"
                                Margin="0,0,0,14"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Click="DashboardItem_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding IconPath}" Width="48" Height="48" />
                                <TextBlock Text="{Binding Label}"
                                           FontSize="20"
                                           FontFamily="Lucida Console"
                                           Foreground="{StaticResource CustomBlackBrush}"
                                           VerticalAlignment="Center"
                                           Margin="20,0,0,0" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
